__pycache__/
*.pyc
.venv/
venv/
.env
*.db
*.sqlite3
# The eval set and its report are evidence, so they are committed. The per-run
# audit logs are intermediate and are not.
eval/results/audit_*.jsonl
dashboard/node_modules/
dashboard/dist/
.DS_Store

# Demo run artefacts. Regenerated on every run; committing them just makes noise
# in the diff.
demo/audit*.jsonl
demo/feedback.jsonl
